name: Test
on: push
jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v3.0.0
      with:
        node-version: '17.x'
    - run: npm install
    - run: npm test
