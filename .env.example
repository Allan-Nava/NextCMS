# Root-level example. The app that actually reads an .env file is cms/ —
# see cms/.env.example for the full, documented list of variables.
#
# Never commit real values here.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
JWT_SECRET=""
ADMIN_URL="http://localhost:4000"
